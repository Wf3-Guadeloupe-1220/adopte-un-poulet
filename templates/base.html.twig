<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}
        {# {{ encore_entry_link_tags('app') }} #}
    {% endblock %}

    {% block javascripts %}
        {# {{ encore_entry_script_tags('app') }} #}
    {% endblock %}
</head>
<body>
<h1><a href="{{ path('home') }}">Adopte un poulet</a></h1>
{% if app.user %}
    <div class="mb-3">
        {{ app.user.email }} <a href="{{ path('app_logout') }}">Se déconnecter</a>
    </div>
{% else %}
    <div class="mb-3">
        <a href="{{ path('app_login') }}">Se connecter</a>
    </div>
{% endif %}

{% block body %}{% endblock %}
</body>
</html>
